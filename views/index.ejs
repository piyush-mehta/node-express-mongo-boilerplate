<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>My App</title>
    
</head>

<body>
    <p>Hello World !! This is a good day</p>
    <ul class="quotes">
            <% for(var i=0; i<quotes.length; i++) {%>
              <li class="quote">
                <span><%= quotes[i].firstname %></span>
                <span>-</span>
                <span><%= quotes[i].quote %></span>
              </li>
            <% } %>
          </ul>
    <form action="/quotes" method="POST">
        <input type="text" name="firstname" placeholder="First Name">
        <input type="text" name="quote" placeholder="Quote">
        <button type="submit">Submit</button>
    </form>
    <div>
        <h2>Replace last quote written by Piyush with a quote written by pm</h2>
        <button id="update"> pm invades! </button>
    </div>
    <script src="main.js"></script>
</body>